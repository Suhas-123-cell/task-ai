# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Backend runtime data (DB, vector store, uploaded resumes)
backend/data/*.db
backend/data/chroma/
backend/data/uploads/
!backend/data/.gitkeep

# Env files
.env
backend/.env
frontend/.env

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
npm-debug.log*

# Combined single-service Docker build output (see repo-root Dockerfile) --
# generated fresh by every build, never hand-edited or committed.
backend/static/

# Locally-sourced textbook PDFs of uncertain redistribution rights: used to
# enrich local RAG demoing, deliberately NEVER committed to this public repo.
# See README's "Using the assigned textbook PDFs" section for the reasoning.

# OS
.DS_Store

# Editors
.vscode/
.idea/
